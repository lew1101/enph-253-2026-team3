syntax = "proto3";

package robot;

import "drive.proto";

message RobotStateSync {
    uint32 sequence = 1;
    uint32 control_flags = 2;
    uint32 session_id = 3;
}

message RobotUartMessage {
    oneof payload {
        RobotStateSync state = 1;
        DriveCommand drive_command = 2;
    }
}
